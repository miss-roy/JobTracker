server:
  port: 8761

spring:
  application:
    name: discovery-server

# A standalone registry should not try to register with or fetch from itself.
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: localhost
