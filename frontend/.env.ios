# Loaded by `vite build --mode ios` (npm run build:ios).
# The iOS Simulator shares the Mac's loopback, so localhost reaches the gateway.
# For a REAL device, change this to your Mac's LAN IP, e.g. http://192.168.1.50:8080
VITE_API_BASE=http://localhost:8080
